[package]
name = "saturating-time"
version = "0.4.0"
authors = ["Clara Engler"]
edition = "2024"
rust-version = "1.85"
description = "A trait for limits and saturations on types inside std::time"
repository = "https://codeberg.org/cve/saturating-time"
license = "MIT"

[features]
nightly = []
